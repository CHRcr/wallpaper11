# AGENTS.md · 项目交接档案

尽量多使用优秀的开源项目

## 一、项目是什么

为**教室 Windows 希沃白板（触屏一体机）**做的可交互动态壁纸，班级自用。仓库：<https://github.com/CHRcr/wallpaper11>

### 原始需求（用户确认过，逐条对照）

1. 时间是必须的，是视觉重心（数字时间，如 12:56）
2. 时间下方：年度进度条（今年过了多少 %），日期放在进度条附近
3. 高考 3500 词循环：**词组形式**（单词 + 变形 + 中文释义 + 短语），随机显示，约 50 秒自动更换，**不要滚动式**，淡入淡出。用户后续提供完整词库文档
4. 设置按钮 + 设置页面，持久化存储
5. 更新由用户在设置页**手动检查和下载**；不做后台/定时自动更新
6. 音乐播放器：工具栏功能按钮，**平时隐藏**；打开后是右上角卡片，不是独立窗口；不内置版权歌曲，本机媒体库自动扫描，也支持网易云搜索（可配置 API）
7. 今日作业展示：拖图片到此处，标题是「今日份作业（美味的小练习~）」——"小练习"是他们学校的梗；要够大、不影响整体、持久化、可清空、无图时不影响美观
8. 性能开销小，不影响教学：有窗口/全屏程序时壁纸停止运行
9. 高考倒计时：2027 年高考，日期待定（现用 2027-06-07 占位，设置里可改）
10. 触屏 + 鼠标交互，不与 Windows 桌面逻辑冲突
11. 设置、播放器等"幸福感功能"收进一个低调工具栏，不显眼

**布局**：桌面左边三列是系统图标区（舞台已让开左 22%）；右下角一小块是希沃管家悬浮区，任何元素不要放那里。

**审美**：不要鸡汤标语；组件风格必须适配背景视频。

## 二、关键决策（已定，别推翻）

- **背景与音乐**：不随程序和 GitHub 仓库分发。用户把背景视频放进 `wallpaper11.exe` 同级的 `media/video/`，音乐放进 `media/music/`；启动或设置页“刷新媒体”会自动扫描。多个视频时按文件名选第一支，建议目标背景命名 `00-background.mp4`
- **主题**：暮色暖金。设计令牌在 `style.css :root`：`--gold: #ffcf9c`、`--rose: #f2a7b3`、烟熏紫玻璃 `--glass-bg*`、暖白 `--ink*`
- **字体**：数字一律 Bahnschrift（`font-stretch: semi-condensed`，时钟 550 字重），中文 Segoe UI/微软雅黑兜底
- **载体**：**Tauri v2**（Rust + WebView2）。发布物是可解压的便携文件夹，程序本体为 `wallpaper11.exe`，媒体目录独立，避免把视频/歌曲塞进单一大 exe；壁纸本体是纯网页 `app/`，宿主负责嵌入、媒体扫描与性能管理
- **桌面嵌入**：WorkerW 方案（向 Progman 发 0x052C 后 SetParent），壁纸层不直接接收输入，交互全在浮层 → 天然不与桌面/触屏冲突
- **更新**：取消计划任务、静默下载与自动覆盖。设置页只保留手动“检查更新”提示和 GitHub 项目入口；媒体文件不会因更新下载、替换或删除
- **持久化**：设置 → localStorage（key: `w11-settings`）；作业图片 → IndexedDB（库 `w11-db`，store `kv`，key `hwImage`，存 Blob）
- **播放器形态**：右上角暖金毛玻璃卡片，默认紧凑展示；歌词、歌单、搜索只在卡片内部展开。除非用户再次明确要求，不改回居中大窗口

## 三、当前状态（已完成 ✅）

`app/` 下纯网页，浏览器打开 `index.html` 即完整可用：

- 大时钟（AM/PM、秒可开关）、年度进度条（日期行 + 发光轨道）、倒计时胶囊（点击直达设置改日期）
- 3500 词卡片：5 组示例数据在 `main.js WORDS`，随机淡换、间隔可调、悬停出 ↻ 手动换、右下角 `n/N` 计数
- 今日份作业：浮层窗口（平时完全隐藏），工具栏按钮/全局拖图自动展开；面板尺寸按图片比例自适应（含 container query 窄面板头部精简）；开板时视频冻结 + 舞台淡出；板头有时钟（晚自习用）；IndexedDB 持久化；有存货时工具栏按钮亮金点
- 设置面板：高考日期/标题、切词间隔、制式、秒、视频冻结、界面缩放、恢复默认
- 音乐播放器：`app/js/player.js` 自研原生音频引擎；工具栏音符按钮打开**右上角暖金卡片**，默认紧凑展示封面、曲名、当前歌词、波形进度和控制键；完整歌词/歌单/搜索在同一卡片内向下展开
  - 本机歌单：宿主扫描 `media/music/`（支持子目录），按文件名最后一个 `-` 拆歌名/歌手；同名 `.lrc` 自动读取。用户复制歌曲后重启或在设置里点“刷新媒体”，无需生成歌单脚本
  - 歌词为内嵌文本，避免 `file://` 下 XHR 受限；当前行会同步到紧凑卡片，打开歌词抽屉后居中显示并支持点行跳转
  - 无专辑封面，本地歌统一用内联 SVG（暮色底+金色音符）避免灰方块；网易云歌用专辑封面（`?param=90y90` 裁小图）
  - **网易云搜索**：面板头部放大镜 → 搜索浮层（防抖 450ms + 回车）；走本地代理 **NeteaseCloudMusicApi**（`tools/netease-api/`，`start.bat` 启动，端口 **16311** 只监听 127.0.0.1）；点结果即播（并行取 `song/url/v1?level=lossless` + `lyric`，加进歌单末尾，按 `ncid` 去重）
  - API 地址是设置项（默认 `http://127.0.0.1:16311`）；**VIP**：设置里填"网易云 Cookie"（MUSIC_U），页面经 `?cookie=` 传参（NeteaseCloudMusicApi 自带能力），无损音质生效；不填则 VIP 歌只有 128k 试听
  - 网易云的 http 直链/封面一律 `replace(/^http:/,'https:')` 升级，避免混合内容
  - 播放器状态存于 `w11-player`：音量、循环模式、上次曲目、抽屉状态、网易云收藏和已移除歌曲
- 性能：浏览器预览在页面不可见时暂停视频；宿主以显式电源状态控制 WorkerW 中的视频；设置可彻底冻结；前台出现其他应用窗口时宿主暂停壁纸
- 调试钩子：`?hw=1` 直接打开作业板；`?music=1` 直接打开音乐面板
- Tauri 2 宿主基础（`host/`）：WorkerW 壁纸窗口 + 透明 overlay 窗口；overlay 只占工具条或当前卡片的实际边界，不再全屏/置顶，不阻挡桌面其余区域与其他软件；托盘设置/暂停/退出；单实例；官方 autostart 插件；检测前台活动窗口，其他应用窗口位于前台时暂停壁纸并隐藏工具条（排除自身窗口与 Windows 桌面层）；`w11stream://` 为 `media/` 中的本机音频/视频提供 Range 读取；Windows Actions 输出便携 zip

## 四、待办（按优先级）

1. **完整 3500 词库**：等用户文档，转成 JSON 放 `app/data/words.json`，`main.js` 改为 fetch 加载（注意 file:// 下 fetch 受限，需兼容 `WORDS` 内嵌兜底）
2. **Windows/希沃实机验证**：先让 GitHub Actions 编译通过，再验证 WorkerW 层级、触控、全屏暂停和便携包启动；Windows 11 桌面层级差异必须按实机结果修正
3. **手动更新发布流程**：以后仅在用户点击设置页 GitHub 项目/更新入口后下载新版本；不下载、不处理 `media/`。当前 `check_update` 只提示手动更新方式
4. **网易云代理宿主化**：Rust 侧起本地 HTTP 转发，或由宿主拉起代理，让搜索开箱即用

## 五、与用户协作的约定（重要）

- **UI 文案克制**：不要"修改即时生效并自动保存"这类冗余描述；不要鸡汤
- 改样式先看 `:root` 设计令牌，保持暮色暖金体系
- 性能预算：动画只用 transform/opacity；新组件先想低配一体机跑不跑得动
- 触屏优先：点击目标 ≥44px，不依赖右键/悬停才能用的核心功能（悬停只能是增强）

## 六、踩过的坑（别再踩）

- `[hidden]` 会被 `display: grid/flex` 覆盖 → CSS 里有 `[hidden]{display:none!important}`，别删
- `file://` 协议下 `fetch()` 相对路径被禁 → 测试加载资源用 `<img>`/`<script>` 标签，或接受内嵌兜底
- PS 5.1 把**无 BOM 的 UTF-8 脚本**按 GBK 读，中文路径直接乱码 → `tools/*.ps1` 必须保存为 **UTF-8 带 BOM**
- 网易云官方 API 没有 CORS 头、且浏览器禁止 JS 手动设 Cookie → 页面**必须走本地代理**（NeteaseCloudMusicApi）；`?cookie=` 传参是它自带的能力，别自己转发 Cookie 头
- 新版 Windows 11 的全屏壁纸 `WorkerW` 可能是 `Progman` 的**子窗口**，不是旧教程中的顶层兄弟窗口；宿主必须优先查找子 `WorkerW`，否则窗口会被系统静态壁纸盖住
- Edge 无头截图：`--headless=new --screenshot=... --window-size=1920,1080 --virtual-time-budget=6000`
